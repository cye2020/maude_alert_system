# config/project_config.yaml
project:
  name: "maude_project"
  root_dir: "."  # 또는 절대 경로
  
paths:
  code_dir: "code"
  loading_dir: "code/loading"
  data_dir: "data"
  config_dir: "config"
  
python:
  add_to_path:
    - "."  # 프로젝트 루트
    - "code"
  
modules:
  loading:
    - data_loader